# Run 20260924-135039 — RUN B (Materials, 9 pages)

**Zip** `Potomac Laser.zip` · SHA-256 `fab7225c…b798db9` (`origin/project-zip` `e1320ef`) · manifest hash matches
**Branch** `build/potomac-laser-fab7225c` (fast-forwarded to `main` `d8be0bc`, then this run's commits)
**Target** potomac-laser.com — LIVE, PRODUCTION MODE · drafts only, nothing published, no global cache clear
**Pattern cap** 20 per run · this run minted **11**
**Zip status: `built`** — all 9 RUN B pages are built

## 9 built / 0 failed

| Design page | Post | Slug | Sections | Verify |
|---|---|---|---|---|
| Materials - Materials.html | **12388** | `pl-auto-materials` | 5 | iter 2 |
| Materials - Adhesives.html | **12389** | `pl-auto-adhesives` | 5 | iter 2 |
| Materials - Glass.html | **12390** | `pl-auto-glass` | 5 | iter 2 |
| Materials - Kapton Polyimide.html | **12391** | `pl-auto-kapton-polyimide` | 5 | iter 2 |
| Materials - Metal Parts.html | **12392** | `pl-auto-metal-parts` | 5 | iter 2 |
| Materials - Plastic Micromachining.html | **12393** | `pl-auto-plastic-micromachining` | 5 | iter 3 |
| Materials - Polymers.html | **12394** | `pl-auto-polymers` | 4 | iter 2 |
| Materials - Stainless Steel.html | **12395** | `pl-auto-stainless-steel` | 4 | iter 2 |
| Materials - Thin Metal Foils.html | **12396** | `pl-auto-thin-metal-foils` | 7 | iter 2 |

Every page opens with the library's `hero-dark-centered` and closes with the new `cta-band-dark-display`. Preview:
`https://www.potomac-laser.com/?page_id=<post>&preview=true`. All nine: `post_type=page` (as the manifest decided),
`post_status=draft`, exact slug (no suffix), `_wp_page_template=default`, stamped `_pl_auto_page` / `_pl_auto_zip` /
`_pl_auto_run`. **Identity (§3.c.i):** no `_pl_auto_page` value starting `Materials - ` existed and none of the nine slugs was
occupied, so all nine were built. Titles are the designs' `<title>` minus the site suffix ("Pressure-Sensitive Adhesives").

## Recovery floor (SKILL.md §0)

Verified, not taken: UpdraftPlus DB set **`1790244496` = 2026-09-24 10:08:16 UTC** (nonce `ec283efbb074`), 3.71 h old at
the gate, `success = 1`, `errors = []`. No backup triggered by this run.

## §7 — 11 patterns minted, 0 discarded

`split-media-rich`, `split-rich-media`, `head-rich-subtle`, `head-rich-white`, `split-rich-rich-related`,
`media-grid-6-note`, `media-grid-4-note`, `tabs-panels-6-note`, `figure-cards-2-note`, `rich-band-white`,
`cta-band-dark-display`. The library now holds 48 fragments.

The Materials designs build most of their sections from the same parts: pill rows, check-lists and card grids in varying
counts. Instead of one pattern per count, the ten section patterns share one **rich-body vocabulary**. Each section has a
single `text-editor` slot, and that widget's `custom_css` styles the classes `pills`, `eb`, `check` (+`narrow`), `cards`
(+`p26`), `related`, `note` and `aside`. Items can then be added or removed without a new pattern (documented under
`split-media-rich` in PATTERNS.md). Copy was converted from the design DOM with BeautifulSoup, not retyped.

Each pattern was tokenised positionally server-side from its first verified occurrence, with 0 mismatches on all 11.
Then came the colour map, a SHA-256 check on download, the legend, the PATTERNS.md entry and coverage row, and
`validate_spec.py --automation` on all 24 specs, which passes clean.

**Dedupe:** `head-rich-subtle` and `head-rich-white` share an element tree with `check-list-centered` (Run A). They were
kept separate because the settings differ: 852px column vs full width, 15.5px vs 16px body, and a check-list-only body vs
the rich vocabulary. The fragment vocabulary has no style tokens, so this follows the `-bold` hero precedent.
`head-rich-subtle` and `head-rich-white` differ only in background colour; they are the first merge candidates if the
library gains a background token.

## ⚠ Correction to run 20260924-125928's report — the closing CTA

That report says Case Studies, FAQs, Quality Policy, Contact and Services & Applications match the design section for
section (±1px). The closing band does not. The designs' `.final-cta` is solid `#0D1B2A` with a 28px grid, a 56px/600 H2,
an 18px lede and 15px buttons, **453px** at 1440+. The library's `cta-band-dark` that Run A used is a 135° gradient with a
dashed eyebrow and a 38px/800 H2, **463px**. Measured this run on FAQs 12300: 463 vs 453. The Run A pages' closing sections
are therefore 10px taller and visibly different in type scale. Run A missed it: its per-page comparison did not flag the
closing band.

This run minted `cta-band-dark-display` for the design's band and used it on all nine Materials pages (453 = design).
**Not done, by rule:** this run may not write to the nine earlier drafts that close with `cta-band-dark`: 12268 and
12277 (run 20260924-100754), and 12295, 12296, 12298, 12300, 12301, 12357 and 12371 (run 20260924-125928). Contact 12302 has
no closing band. Swapping their closing section to `cta-band-dark-display` is a human (or supervised) pass. The service
drafts 12239–12243 also close with `cta-band-dark`; this run did not compare them against their designs.

## §3.c.vi verify — all 9 pass

Verification ran in the user's logged-in Chrome, in a tab of my own group (closed afterwards), with the mock rendered in
the same browser. Checks per page:

- section heights against the mock
- a word-level text diff (server-rendered via Elementor, against the mock's text)
- image load state
- no literal `{token}`
- behaviour tests for anything interactive

**Iteration 1 → 2 fixes (all nine pages rewritten):**

- the closing band (above)
- 26/24 card padding, which the design sets inline per card (`p26`; Glass, Adhesives, Thin Metal Foils: 2px per row)
- `.ab-prose p` keeping its 16px margin on the last paragraph and wrapping `pretty` (Materials −15)
- paragraph class taken from the design's inline style rather than a text regex, so Stainless's 760px note and
  check-list became `note`/`narrow` (−33) and Plastic's closing line became `aside` (−4)

**Iteration 3 — Plastic Micromachining tabs.** Iteration 2 switched `aria-selected` correctly, but tabs 2–6 showed **no
panel**. The panels are hidden by their own `custom_css`, and the script cleared the inline style instead of setting
`display:flex`. After the fix, real clicks and scripted clicks each show exactly their own panel. The design has no
arrow-key navigation, and neither does the build.

**Geometry after the fixes** (section heights, build vs design):

| Page | Result |
|---|---|
| Materials | exact except split §1 668 vs 667 |
| Glass | exact |
| Kapton | exact except §3 566 vs 565 |
| Metal Parts | +8 on §1 only, from one brand-normalised line (see below) |
| Plastic | §1 746 vs 744; tabs 643, grid 549 and CTA exact |
| Polymers | exact |
| Stainless | exact |
| Adhesives | +30 (§1) and +45 (§3), from brand-normalised lines |
| Thin Metal Foils | +45 (§4) from a brand-normalised H2, otherwise exact |

The brand-normalised deltas come from "Goodfellow Microfabrication" being longer than "Potomac", which adds a line.
Tab panels differ where brand-normalised copy wraps (PEEK, Nylon).

**Text:** after brand normalisation, the diff is exact on all nine pages; every remaining difference is a brand swap.
**Post-write gates:** after every `_elementor_data` write the run re-read and decoded the data, deleted the element cache
and `_elementor_css`, regenerated the per-post CSS and ran `rocket_clean_post`. Live `/`, `/services/cnc-micro-machining-services/`,
`/contact/`, `/about/` and `/materials/` return 200, and no live page carries `cnc-elementor-fullbleed`.

## Images — 16 new attachments, 5 reused

New: 12372–12387, all `pl-auto-mat-*`, sideloaded from originals in `uploads/` with a source-hash check. Reused
(hash-identical): 12303 (twice), 12304, 12309 from run 20260924-125928, and 12378 within this run (Stainless reuses a Metal
Parts photo). Alt text comes from the design.

## §3.d link pass

Design hrefs on all nine drafts now point at drafts or live pages:

- the nine Materials pages → each other's drafts
- Contact → 12302
- Project Gallery → 12357
- Services - Laser Micromachining → 12240 (`was=` identity; linking to it, not writing it)
- **Unbuilt targets → live counterparts:** Blog - Kapton.dc.html → `/blog/machining-kapton-for-aerospace/` (post 12205,
  exact title); Applications - CCIT AW.html → `/application/leak-test-hole-drilling/` ("CCIT Leak-Test Calibration")

Element ids are verified unchanged, and no `.html` href remains. The spec token values carry the same URLs.

## Copy decisions to review

- **Brand normalisation** (present tense only): 16 strings, including the nine "Potomac Photonics" card eyebrows on Materials, "Why
  choose Goodfellow Microfabrication…" and body copy. **Kept verbatim (merger narrative):** "Potomac Photonics has
  joined the Goodfellow family", "Potomac and Goodfellow", "Potomac Photonics now combines…", "Together, Potomac's
  microfabrication…", and "Potomac Photonics, *now part of Goodfellow*" on Thin Metal Foils. That last one was normalised
  in v1 because the phrase spans a link, and was restored before verify with a markup-aware KEEP key.
- **Content Style Rules breaches kept for the designer** (§7 pre-publish check on the rendered text; no dashes or arrows
  found):
  - "Why choose…" headings on Adhesives and Thin Metal Foils (a banned shape)
  - conversational openers "What customers bring to us" (Plastic) and "What makes a steel stainless" (Stainless)
  - middle-dot chains in body "Related:" lines on Kapton, Metal Parts, Plastic, Polymers and Thin Metal Foils (dots are
    permitted only in masthead chips)
- **Hero accent spans:** the design's `<span class="accent">` in each H1 renders white-on-white (`.ab-hero__title .accent{color:#fff}`),
  so it was dropped. The render is identical, and no single-accented-word heading appears.

## §8 drift report — informational only, nothing modified

The live counterparts are **`post_material`** posts, none built with Elementor: Glass 108, Kapton 8433, Metal Parts 5550,
Polymers 1668, Pressure-Sensitive Adhesives 10956, Stainless Steel 2152, Thin Metal Foils 10958 (12 published
`post_material` in total). Materials is the page 96 (`materials`). No slug match was found for Plastic Micromachining.
`post_material` is not a type SKILL §1 allows, so the drafts are pages (`/pl-auto-*/`), not `/material/…` URLs; going
live would mean a human moves content onto the `post_material` posts or changes the permalinks.

## Findings for a human

1. **Closing CTA on 9 earlier drafts** (12268, 12277, 12295, 12296, 12298, 12300, 12301, 12357, 12371). Swap to
   `cta-band-dark-display` to match the designs (see the correction above), and check the service drafts 12239–12243
   against theirs.
2. **`post_material` vs `page`** for going live (§8).
3. **Style-rule breaches** above go back to the designer.
4. **Merge candidates** if the fragment vocabulary grows style tokens: `head-rich-subtle`/`head-rich-white`, and the
   `-bold` hero pair from Run A.
5. Carried over: the HubSpot test submission on 12302, and re-pointing the links on 12268/12277.

## Rollback map

`built/run-20260924-135039.manifest.json` lists everything this run created:

- posts **12388–12396** (drafts)
- **16** attachments, 12372–12387
- under `novamira-drafts/run-20260924-135039/`: the bundles (v1, TMF v2, v2, Plastic v3, link), the tokenise job and the text renders
- 11 fragment files in `novamira-drafts/patterns/`

One unused upload token was minted for a wrong-root path (`public_html/novamira-drafts/…`); it was never used and nothing
was written there. Writes were limited to this run's own posts and attachments. There was no global operation.
