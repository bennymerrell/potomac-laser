# Run 20260924-150733 — supervised: rebuild the 8 material pages as `post_material` drafts

**Requested in chat.** After it turned out that the Materials drafts were `page` posts while their live equivalents are
`post_material`, the user enabled Elementor for Materials (Elementor → Settings → Post Types) and asked for a rebuild.
Target: potomac-laser.com, LIVE. Drafts only, nothing published, and no live post touched. §0: UpdraftPlus DB set
`1790244496` (nonce `ec283efbb074`), verified at 4.98 h old.

## New drafts

| New draft | Rebuilt from page draft | Replaces live | Parent |
|---|---|---|---|
| **12397** `pl-auto-adhesives` | 12389 | 10956 `/material/pressure-sensitive-adhesives/` | — |
| **12398** `pl-auto-glass` | 12390 | 108 `/material/glass/` | — |
| **12399** `pl-auto-kapton-polyimide` | 12391 | 8433 `/material/kapton-polyimide/` | — |
| **12400** `pl-auto-metal-parts` | 12392 | 5550 `/material/metal/` | — |
| **12401** `pl-auto-plastic-micromachining` | 12393 | 2502 `/material/plastics/` | — |
| **12402** `pl-auto-polymers` | 12394 | 1668 `/material/plastics/polymers/` | 2502 |
| **12403** `pl-auto-stainless-steel` | 12395 | 2152 `/material/metal/stainless-steel/` | 5550 |
| **12404** `pl-auto-thin-metal-foils` | 12396 | 10958 `/material/thin-metal-foils/` | — |

Preview: `https://www.potomac-laser.com/?post_type=post_material&p=<id>&preview=true`.

- **Content:** each draft is its page draft's verified `_elementor_data`, copied with a hash guard. Links between Materials
  pages were re-pointed to the new drafts. The Materials hub, page draft **12388** (live equivalent: page 96
  `/materials/`, so it stays a page), had its 6 card links re-pointed too; its rollback file is saved.
- **Stamps and parents:** each draft carries the same `_pl_auto_page` stamp as its page draft, plus `_pl_auto_run` = this
  run and `_pl_auto_rebuilt_from` = the page draft id. Menu order is copied from the live material, and each parent
  matches the live one.
- **Page drafts kept:** 12389–12396 are **unchanged**, pending a decision. Until they're trashed, two drafts carry each
  of those design paths; §3.c.i treats any hit as "already built", so a future run still skips them.

**Snippet #10** (site code, approved in chat): the full-bleed treatment now also covers stamped `post_material` drafts.
They are exempt from the 1224px inner cap, as page drafts are. The material template's title band, trailing spacer and
"POTOMAC RPM / Rapid Response" band are hidden, so each page ends with the design's own closing band. The first save
still capped the inner width at 1224px, which widened the text columns (Glass 564/913 vs 539/866); the second save
fixed it. Only posts stamped `_pl_auto_page` are affected: live `/material/glass/`, `/material/plastics/polymers/` and
`/materials/` return 200 without the full-bleed class. Before/after code is in `reference/code-snippets/`
(`snippet-10.{before,after}-20260924-150733.php`).

**Verified** in the user's Chrome (own tab group, closed afterwards):
- all eight have section heights **equal** to the verified page drafts
- inner widths match the design (1200/1152/…/720)
- no template title and no extra band
- no broken images and no stale links
- the Plastic tabs work
- service draft 12239 keeps its 1224px cap

**Repo:** SKILL.md §1, AUTOMATION.md (§2 manifest parser, §3.c.i identity query, post-type rule, report schema),
`validate_spec.py`, the manifest (8 lines → `post_material`; the hub stays `page`) and the 8 specs now allow or name
`post_material`. `validate_spec --automation` is clean on all 24 specs. The ledger's Run B entry now points at 12397–12404
(each with `superseded_page_draft`).

## Rollback

- trash drafts 12397–12404
- restore `built/run-20260924-150733.rollback-12388.json` to 12388
- restore `snippet-10.before-20260924-150733.php` as Snippet #10's code

The manifest lists every file.
