# Run 20260924-125928 — RUN A re-entry (partial zip, 8 failed pages)

**Zip** `Potomac Laser.zip` · SHA-256 `fab7225c…b798db9` (`origin/project-zip` `e1320ef`) · manifest hash still matches
**Branch** `build/potomac-laser-fab7225c` (fast-forwarded to the merged `main`, then this run's commits)
**Target** potomac-laser.com — LIVE, PRODUCTION MODE · drafts only, nothing published, no global cache clear
**Pattern cap** 20 (`# pattern_cap: 20` in the manifest, counted per run — AUTOMATION.md §7.9 as revised on `main`)
**Zip status: `built`** — all 10 RUN A pages are built (2 by run 20260924-100754, 8 by this run)

## 8 built / 0 failed · §2 re-entered only the 8 pages that failed on the budget

| Design page | Post | Slug | Sections | New patterns | Verify |
|---|---|---|---|---|---|
| About - Case Studies.html | **12295** | `pl-auto-case-studies` | 3 | `hero-dark-centered`, `image-link-cards-5` | iter 2 |
| About - News.html | **12296** | `pl-auto-news` | 3 | `image-link-cards-12-button` (+ reuses `hero-dark-centered`) | iter 2 |
| About - Employment.html | **12298** | `pl-auto-employment` | 4 | `hero-dark-centered-cta`, `split-prose-list-media`, `text-link-cards-6-filter` | iter 2 |
| About - FAQs.html | **12300** | `pl-auto-faqs` | 3 | `faq-accordion-filter-10` (+ reuses `hero-dark-centered`) | iter 2 |
| About - Quality Policy.html | **12301** | `pl-auto-quality-policy` | 4 | `split-list-figure`, `check-list-centered` (+ reuses `hero-dark-centered`) | iter 1 |
| Contact - Contact.html | **12302** | `pl-auto-contact` | 3 | `hero-split-contact-form`, `hq-panel-map`, `offices-grid-6-social` | iter 2 |
| Gallery - Project Gallery.html | **12357** | `pl-auto-project-gallery` | 3 | `hero-dark-centered-meta-4`, `gallery-grid-filter-lightbox` | iter 2 |
| Services - Services & Applications.html | **12371** | `pl-auto-services-and-applications` | 6 | `hero-dark-centered-meta-4-bold`, `service-cards-5-caps`, `service-cards-6-button`, `estimate-band-split`, `feature-cards-2-image` | iter 2 |

Every page closes with the library's `cta-band-dark` except Contact (the design hides its newsletter band).
Preview: `https://www.potomac-laser.com/?page_id=<post>&preview=true`. All eight: `post_type=page`, `post_status=draft`,
exact slug (no suffix), `_wp_page_template=default`, stamped `_pl_auto_page` / `_pl_auto_zip` / `_pl_auto_run`.

**Identity (§3.c.i)** used the corrected postmeta query (AUTOMATION.md fix on `main`): none of the 8 paths was stamped and
none of the 8 `pl-auto-` slugs existed, so all 8 were built.

## Recovery floor (SKILL.md §0)

Verified, not taken: UpdraftPlus DB set **`1790244496` = 2026-09-24 10:08:16 UTC** (nonce `ec283efbb074`, taken by the previous
run), 2.85 h old at the gate, `success = 1`, `errors = []`. No backup triggered by this run.

## §7 — 19 patterns minted (cap 20), 0 discarded, 3 dedupe hits

Minted in page order; each authored from getComputedStyle measurements of the design mock at 1440px, built, verified, then
**tokenised positionally** server-side: the built section and the authored template are walked in lockstep, tokens placed at
the template's positions, and **every other leaf must be identical** (0 mismatches on all 19), which proves the built post is
exactly the fragment. Then the colour map (`custom_css` Kit hexes → `var(--e-global-color-*)`, colour controls → `__globals__`),
write to `novamira-drafts/patterns/`, download, SHA-256 check, legend, PATTERNS.md entry + coverage row, `validate_spec.py
--automation` clean on all 8 specs. Positional rather than value-based because values repeat ("Read the case study" ×5).

**Dedupe hits:** `hero-dark-centered` reused by News, FAQs and Quality Policy. **Not deduped:** the Services & Applications
hero shares `hero-dark-centered-meta-4`'s element tree but differs in weight, size, gaps and eyebrow colour; the fragment
vocabulary has no style tokens, so it is a separate `-bold` variant (stated in its entry).

**One tokenisation corrected before commit:** `hero-split-contact-form` first tokenised its form's success message as
`body_*`; the validator rejects a `body_*` slot with no block markup. The token kind was changed to `heading_*`, the
re-assembled page was **byte-identical** to what was already on post 12302 (so the post was not rewritten), the first
fragment file was deleted, and the pattern re-tokenised. §7.8 reads "validator failure → discard"; I treated this as a
tokenisation defect caught before anything entered the library, not a failed build — flagging it so a human can disagree.

## §3.c.vi verify — all 8 pass (7 on iteration 2, 1 on iteration 1)

In the user's logged-in Chrome at 1792px; the mock rendered in the same browser/width. Per page: section heights vs the mock,
card/image/control geometry, computed typography, a word-level text diff (rendered server-side with Elementor, compared
against the mock's text with the brand normalisation applied), image load state, no literal `{token}` — and **behaviour
tests for everything interactive** (AUDIT.md B15):

- **Employment** — chip filter: Sales → 3, HR → 1, Engineering → 1, All → 6; `aria-pressed` tracks. *Iteration 1 was inert*
  (the script collected the cards before the grid below it existed); fixed by querying at click time.
- **FAQs** — filter Technical 4 / Ordering 6 / All 10; a question opens (73 → 121px) and its "+" rotates to "×". *Iteration 1
  rendered no "+" circle* (an empty toggle icon makes Elementor omit the element), rows 65 not 73px.
- **Project Gallery** — Metal Micromachining → 8 with "Showing 8 projects", Diamond → 2, All → 54; lightbox opens on the
  clicked card ("2 / 2", category, title, image), next steps within the filtered set, Esc closes and restores scroll. *Iteration
  1 was inert* (script scoped to the bar's own container — `closest(".e-parent")` — instead of the page section).
- **Contact** — form validation: an empty submit and a bad email each show the in-card error; nothing was sent.

Geometry results: Case Studies, FAQs, Quality Policy, Contact and Services & Applications match the design section for
section (±1px); Employment and News match except the hero, taller by exactly one H1 line because the brand-normalised title
wraps; the Gallery is 26px taller over 5.9k (filter bar + count line, <0.5%). Text: exact on every page, allowing for
whitespace artefacts, the hidden FAQ category labels (10 words), placeholder attributes, and Elementor's screen-reader icon
names on Contact.

Post-write gates after every `_elementor_data` write: re-read + `json_decode`, element cache deleted, `_elementor_css` deleted,
per-post CSS regenerated, `rocket_clean_post`. Live homepage, `/services/cnc-micro-machining-services/`, `/contact/`,
`/project-gallery/`, `/about/` → 200, no errors, no `cnc-elementor-fullbleed` class on any live page.

## Forms and external services

`hero-split-contact-form` is **wired to HubSpot** (Forms API v3, portal `143181153`, form `[Potomac] Contact Form`
`54d0ec75-106d-43eb-a420-0ff22e448e6d`; fields `email firstname lastname phone company website potomac_message` + `hs_lead_status:
NEW` + UTM; `context.hutk`; PII-safe `lead_form_*` analytics). The design's own handler only faked success. **Not yet proven
end-to-end:** a test submission creates a real contact in the live portal, which reference/HUBSPOT-FORMS.md reserves for a human.
The Google Maps iframe is the design's own embed.

## Images — 86 new attachments, 0 deduped

12278–12294 (case study + news cards), 12297 (Employment photo), 12299 (ISO certificate), 12303–12356 (54 gallery cards),
12358–12370 (Services & Applications) — all `pl-auto-`-prefixed, sideloaded from the originals already in `uploads/` with a
source-hash check; alt text from the design where it has one (Employment photo alt brand-normalised; the ISO certificate alt
kept "Potomac Photonics" — it describes a document issued in that name).

## §3.d link pass

Design page hrefs on all 8 new drafts now point at drafts: CNC → 12239 (and `#quote`), About Our Group → 12268, About Potomac →
12277, Contact → 12302, Project Gallery → 12357, Services & Applications → 12371 — including links inside html widgets (the
gallery lightbox CTAs). Element ids verified unchanged; no `.html` href remains. **Not done, by rule:** 12268 and 12277 belong
to run 20260924-100754, so this run did not write to them; their Gallery/Contact/Services & Applications links still point at
the live pages and should be re-pointed to 12357/12302/12371 by a human (or a supervised pass).

## Copy decisions to review

- **Brand normalisation** (present tense only): hero H1s/ledes on Case Studies, News, Employment ("Careers at Goodfellow
  Microfabrication"), the Employment eyebrow and CTA button ("About Goodfellow Microfabrication"), FAQ question 10.
  **Kept verbatim (history):** case-study titles and text naming Potomac Photonics / Potomac Mesosystems, all 12 dated news
  headlines and summaries, the ISO certificate alt.
- **Content Style Rules breaches kept for the designer:** "→" in Contact ("Request a quote →", "Send message →", "Email the
  team →") and in 11 Services & Applications card labels; the gallery chip counts are static copy (keep in step with cards).
- **Tweak defaults applied** (Contact, Gallery) — the rendered design, not the file's raw markup, is what was built.
- **Icons:** Contact's inline SVGs became Font Awesome in native widgets; social-icon screen-reader labels read "facebook-f"
  etc. rather than the design's aria-labels.

## §8 drift report — informational only, nothing modified

Unchanged from run 20260924-100754's report (same counterparts): Case Studies → 391 (`case-studies`, not Elementor);
Employment → 6275 / 11321 staging / 11235 private; FAQs → 118; News → 465; Quality Policy → 640; Contact → 15, 12219 staging,
1417; Project Gallery → **12183** (`project-gallery`, 3 sections — same count as the design) and 7 draft; Services &
Applications → 9970 (`services-applications`, not Elementor). New pipeline drafts carrying `_pl_auto_page`: 12295, 12296,
12298, 12300, 12301, 12302, 12357, 12371.

## Findings for a human

1. **HubSpot test submission** for the Contact form (12302) — needs your go-ahead; everything short of sending is tested.
2. **Re-point 12268/12277's links** to the new drafts (this run may not write to them).
3. **Services & Applications hero vs gallery hero** are the same tree with different settings — if the library should grow
   style tokens, these two are the first candidates to merge.
4. **RUN B** (Materials, 9 pages) is next in the manifest. The zip is now `built`, so advancing needs the human reset
   (AUTOMATION.md §5) — delete this zip's ledger entry — after uncommenting the block. With the 39-pattern library, several
   Materials sections should now match.

## Rollback map

`built/run-20260924-125928.manifest.json`: posts **12295, 12296, 12298, 12300, 12301, 12302, 12357, 12371** (drafts);
**86** attachments (listed individually); files under `novamira-drafts/run-20260924-125928/` (bundles, job files, text
renders) and 19 fragment files in `novamira-drafts/patterns/`. Writes were limited to this run's own posts and attachments;
no global operation this run (the §0 set was verified, not taken).
