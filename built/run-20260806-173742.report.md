# Run 20260806-173742 — autonomous page-sync run

**Zip** `Potomac Laser.zip` · SHA-256 `2f5215b4…f331e14e` (from `origin/project-zip`, 12,249,689 bytes)
**Branch** `build/potomac-laser` · **Coordinator branch** `main-2` (level with `origin/main` at start)
**Target** potomac-laser.com — LIVE, PRODUCTION MODE
**Manifest** `manifests/potomac-laser.txt` — resolved on `# zip:` filename; `# zip_sha256:` **still matches**, so no "manifest predates this zip revision" warning.

This was the first autonomous coordinator run. The three service pages built on 2026-08-06
under human supervision were already stamped `_pl_auto_page`, so §3.c.i skipped them; the run
built the two remaining pages of the manifest's active phase.

## 2 built / 0 failed / 3 skipped-already-built

| Design page | Post | Type | Slug | Sections | Status |
|---|---|---|---|---|---|
| Rapid Prototyping.html | **12242** | `post_services` | `pl-auto-rapid-prototyping` | 12 | built |
| 3D Printing.html | **12243** | `post_services` | `pl-auto-3d-printing` | 12 | built |
| CNC Micromachining.html | 12239 | `post_services` | `pl-auto-cnc-micromachining` | 10 | skipped-already-built |
| Laser Micromachining.html | 12240 | `post_services` | `pl-auto-laser-micromachining` | 12 | skipped-already-built |
| Micro-Hole Drilling.html | 12241 | `post_services` | `pl-auto-micro-hole-drilling` | 12 | skipped-already-built |

Preview URLs (logged-in):
`https://www.potomac-laser.com/?post_type=post_services&p=12242&preview=true`
`https://www.potomac-laser.com/?post_type=post_services&p=12243&preview=true`

Post type came from the manifest (`post_services`), gated against `elementor_cpt_support`
(`["page","post_services","post_application"]`) before either insert. Both slugs came back from
`wp_insert_post` unsuffixed, so nothing already occupied them.

## Recovery floor (SKILL.md §0)

Gate **passed on an existing set — no backup was taken.** Newest UpdraftPlus set
`1786020030` = **2026-08-06 12:40:30 UTC**, 4.95 h old at the gate, `updraft_last_backup['success'] = 1`,
`errors` empty. Nonce `3740600017dd`, file `backup_2026-08-06-0840_Potomac_Laser_3740600017dd-db.gz.crypt`.
`updraft_delete_local=1` with `service=['dropbox']`, so that set exists on Dropbox only —
there is no local copy to fall back on.

Second §0 gate: the production server `novamira-potomac-laser-co` answered; `novamira-localhost`
is configured in `.mcp.json` but refused connection (ECONNREFUSED), which is the
configured-but-unreachable case the gate explicitly permits.

## §3a enumeration

The manifest is the complete set, and all five of its active paths exist in the zip. **23 HTML
files in the zip; 5 built, 18 unlisted.** Unlisted files are reported, never built:

| Unlisted file | `<section>` count |
|---|---|
| `3D Printing (standalone).html` | 12 |
| `About Our Group.html` | 4 |
| `Blog Article.dc.html` | 2 |
| `Blog Index.dc.html` | 5 |
| `Canvas-4.dc.html` | 0 |
| `CCIT.html` | 9 |
| `Contact.html` | 4 |
| `design_handoff_about_page/About Our Group -no hero-.html` | 3 |
| `Kapton.dc.html` | 2 |
| `Project Gallery.html` | 3 |
| `Services & Applications.html` | 6 |
| `Site Footer.dc.html` | 0 |
| `_src/original.html` | 0 |
| `uploads/CNC Micromachining (standalone).html` | 12 |
| `uploads/Project Gallery (standalone).html` | 3 |
| `uploads/cnc-standalone-upload.html` | 12 |
| `uploads/cnc-unpacked.html` | 12 |
| `uploads/sector-medical-potomac.html` | 6 |

Seven of those are the manifest's phased RUN 2 / RUN 3 pages (`About Our Group`, `Contact`,
`Project Gallery`, `Services & Applications`, `CCIT`, `uploads/sector-medical-potomac`) plus the
two blog canvases the manifest keeps as known-good intent with no covering pattern. The rest are
the duplicates and working copies the manifest documents. **No fallback heuristic ran** — a
manifest resolved, so nothing was excluded by rule.

## §3.c.ii match table

Both pages are **STANDARD BUILD — 12 of 12 matched, zero `UNMATCHED`, no §7 authoring, no new
patterns minted.** Matching was not assumed from the sibling pages: a per-section structural
signature (tag + class skeleton, text ignored) was computed for both pages and for the verified
Micro-Hole Drilling page.

| # | Section | Pattern | Signature vs MHD | Design images | Fragment `{image_n}` |
|---|---|---|---|---|---|
| 0 | `hero` | `hero-dark-stat-strip` | differs only in `<br>`/`<span>` order inside the H1 (copy, not structure) | 0 | 0 |
| 1 | `why-us` | `why-choose-inset-cta` | identical | 1 | 1 |
| 2 | `services` | `services-image-cards` | identical | 5 | 5 |
| 3 | `applications` | `interactive-iframe-embed` | identical | 0 | 0 |
| 4 | `capabilities` | `spec-table-dark` | identical (RP); 3DP 96 vs 98 tags, same 8 rows and same 24 content nodes — rows are adjustable per the pattern's Notes | 0 | 0 |
| 5 | `process-selection` | `process-comparison-cards` | identical | 0 | 0 |
| 6 | `process` | `process-steps-numbered` | identical | 0 | 0 |
| 7 | *(group)* | `group-ecosystem-cards` | identical | 4 | 4 |
| 8 | *(testimonials)* | `testimonials-avatar-cards` | identical | 0 | 0 |
| 9 | `quote` | `quote-form-hubspot` | identical | 0 | 0 |
| 10 | `faq` | `faq-toggle` | identical | 0 | 0 |
| 11 | *(final CTA)* | `cta-band-dark` | identical | 0 | 0 |

**Image coverage checked per §3.c.ii, not skipped:** the three image-bearing sections need 1, 5
and 4 slots and their fragments supply exactly 1, 5 and 4. No section had more images than its
fragment could carry, so nothing was sent to §7 on image coverage and nothing was match-and-dropped.

Structural `options` are fragment-native: `group-ecosystem-cards.you_are_here_unit = 2` and
`process-steps-numbered.highlight_step = 3` both equal post 12133's own arrangement, and both
pages' designs want exactly that — the RP and 3DP markup carries "You are here" on the Precision
Fabrication card and `gf-workflow-step active` on step 03. The assembler refuses any other value
rather than silently ignoring it.

## Interactive assets (§3.c.iv)

Both resolved by **path 1 — a file already on the server, referenced and never modified**:

| Page | File | Bytes | Server mtime | SHA-256 |
|---|---|---|---|---|
| 12242 | `novamira-drafts/rp-interactive.html` | 107,796 | 2026-07-30 15:37:36 UTC | `a1a5de27…1027ef` |
| 12243 | `novamira-drafts/3dp-interactive.html` | 107,479 | 2026-07-30 15:27:52 UTC | `414d2035…f5f9b8f` |

Self-containment gate: **zero** `<script src="http…">` and **zero** external stylesheets in either.
Each carries one `<link rel="canonical">`, which is inert inside an iframe — reported, not failed,
exactly as the gate prescribes. Frame ids come from the file names (`rp-interactive-frame`,
`3dp-interactive-frame`), not the `pl-auto-` slugs. No section was recorded `deferred-interactive`.

**Carried forward from the manifest's own caveat:** these clusters date from 2026-07-30/31 and were
built for the previous iteration of these pages. This design rewrites per-page explorer content
(the inline `APPS` object: summaries, chips, materials). Both iframes load and work, but their copy
may be stale until someone diffs the `APPS` data against the design. This run referenced them
because §1 forbids editing files it did not create; refreshing them is a human's new file.

## Images — 20 slots, 0 uploads

Every image on both pages is **byte-identical (SHA-256) to the corresponding upload from run
20260806-124030**, so all 20 slots deduped to attachments **12229–12238**. Zero new attachments,
zero new media files. Both id gates passed on both shapes: no `image` widget without an `id`, and
the `why-choose-inset-cta` photo column's container `background_image` carries id 12234 (the B12
shape the narrower gate could not see).

## §3.c.v build

`_elementor_data` was staged as a file under `novamira-drafts/` and verified by SHA-256 server-side
before the meta write, so a truncated transfer could not become a half-written page:

| Page | File | Bytes | SHA-256 |
|---|---|---|---|
| 12242 | `pl-auto-rapid-prototyping.elementor.json` | 141,910 | `e9b2f05e…b9a33c16` |
| 12243 | `pl-auto-3d-printing.elementor.json` | 141,717 | `65da2a1d…1e98c889` |

After **every** `_elementor_data` write — the first and the fix-loop rewrite, four writes in all —
all four invalidation steps ran for that post only: `_elementor_element_cache` deleted,
`_elementor_css` deleted, `Post::create($id)->update()`, `rocket_clean_post`. Confirmed after each:
element cache absent, `_elementor_css` regenerated. No global cache clear at any point.

Post-write gates: `_elementor_data` re-read and `json_decode`d to 12 sections on both pages, both
times. Live-site spot-check (homepage + `/services/cnc-micro-machining-services/`) returned 200
with expected titles both before and after the writes.

### New tool: `tools/assemble.py`

Assembly had no committed implementation, so this run added one and **proved it against known-good
output before using it**: `--self-test` re-assembles the verified Micro-Hole Drilling spec and
compares it to `built/pl-auto-micro-hole-drilling.json` with element ids normalised. It reproduces
that file exactly. The self-test also surfaced that the two §7 fragments
(`services-image-cards`, `quote-form-hubspot`) are one-element arrays while the ten originals are
bare section objects — the assembler unwraps both.

## §3.c.vi verify — 2 iterations of 3, both pages pass

Verification compared the draft preview against `specs/<slug>/mock.html` (the design page with its
ten data-URI images rewired to relative asset paths and the Tailwind Play CDN, unpkg React builds
and babel tweaks panel replaced by the compiled stylesheet) on three axes: per-section normalised
text hashes line by line, card-grid geometry, and screenshots.

**Iteration 1 found a real defect.** `services-image-cards` **interleaves** description and link
per card — `body_3`/`body_4` are card 1's description and its "Learn more" link, `body_5`/`body_6`
card 2, and so on. Both specs had been written from a sibling spec that **grouped** them
(`body_3`–`body_7` descriptions, `body_8`–`body_12` links), so every card's link slot rendered the
*next* card's description and cards 4 and 5 lost their descriptions entirely. Nothing mechanical
caught it: every token was filled, no token syntax survived, the validator passed, and the element
count matched — it was visible only in the rendering.

**Iteration 2** rebuilt both specs with the token order read off the fragment's own document
order. Every other pattern was audited the same way at the same time (fragment token order vs
design node order, all twelve sections, both pages) and the rest were correct, including
`group-ecosystem-cards`, whose cards 2 and 4 genuinely have no description slot.

Result after iteration 2, both pages:

- **Exact text match** on `services-image-cards`, `spec-table-dark`, `process-steps-numbered`,
  `testimonials-avatar-cards`, `cta-band-dark` — identical line-hash sequences.
- **Accounted-for differences** on the rest, verified by line-level set difference rather than
  assumed: the only line present in the build but not the design is `Request a Quote`, and the only
  design lines missing from the build are the matching `Request Material + Machining Quote`, the
  hero's "No redesign required to start…" sentence, the HubSpot form's own field labels, and the
  eight FAQ answers that `innerText` cannot see while the toggles are collapsed.
- **Geometry:** service cards 5-across at 223 px each, comparison cards 3-across at 369 px,
  ecosystem cards 4-across at 283 px — no collapse to content width. All 12 top-level containers
  have non-zero height. 9 of 9 images load; 0 broken. Iframes 1792×3122 (RP) and 1792×3148 (3DP).
  8 FAQ toggle items. No literal `{token}` anywhere in the rendered text.

### Two deltas left in place deliberately, both inherited from the fragments

Neither is a defect in these pages, and fixing either means editing a fragment, which §8 forbids
outright:

1. **`Request a Quote` vs the design's `Request Material + Machining Quote`** on `button_2` of
   hero / why-choose / process-comparison and `button_1` of group-ecosystem. The three sibling
   pages built and verified earlier use the short label; matching them keeps the five-page cluster
   consistent. Changing it is a one-token edit per spec if the shorter label was not intended.
2. **The hero's second reassurance line** ("No redesign required to start — we'll help optimise
   your part.") has no slot: `hero-dark-stat-strip` defines three body tokens and the design
   section has four text nodes. Present on all five siblings. A `hero-dark-stat-strip-*` variant
   with a fourth body slot would close it.

One cosmetic difference worth a human's eye, also fragment-level and shared with all siblings: the
eyebrow marker renders as a filled orange rectangle where the design draws a short rule, and
`EXPLORE SERVICES →` stacks below the buttons where the design sets it inline.

## §3d link pass — skipped, correctly

**Zero** `href` values pointing at a `.html` file in the export, on either page. The design's
internal links are canonical production permalinks (`/services/3d-printing/`,
`/services/laser-micromachining/`, `/services/rapid-prototyping/`, …) which already resolve to live
pages. There are no export-internal cross-links to rewrite, so §3d's own skip condition applies —
no preview URLs were baked into either draft. (Had they been rewritten, the go-live slug rename
would have had to undo it, and the built pages would have diverged from the mocks they were just
verified against.)

## §7 — not triggered

No `UNMATCHED` section on either page, so no candidate was authored, none was minted, none was
discarded, and the 8-per-zip cap was not approached. `PATTERNS.md`, the fragments and the legends
are untouched. Both pages reused `services-image-cards` and `quote-form-hubspot`, minted from post
12239 by the earlier run.

## §8 drift report — informational only, nothing modified

Slug-family and title searches for all five pages of the active phase. Non-page types
(`work`, `blog`, `case_study`, `news`, `wpseo_crawl_issue`, …) matched on keywords in bulk and are
summarised rather than listed; the page-like counterparts are:

**Rapid Prototyping.html** — design has 12 sections
| Post | Type | Status | Slug | Elementor sections | Modified | `_pl_auto_page` |
|---|---|---|---|---|---|---|
| 11536 | `post_services` | publish | `rapid-prototyping-services` | 0 (not Elementor) | 2026-03-06 | – |
| 12224 | `post_services` | publish | `rapid-prototyping-services-draft-blocks` | **9 ≠ 12** | 2026-07-30 | – |
| 12242 | `post_services` | draft | `pl-auto-rapid-prototyping` | 12 | this run | ✅ |

**3D Printing.html** — design has 12 sections
| Post | Type | Status | Slug | Elementor sections | Modified | `_pl_auto_page` |
|---|---|---|---|---|---|---|
| 4958 | `post_services` | publish | `3d-printing` | 0 | 2026-03-06 | – |
| 5616 | `post_services` | publish | `what-3d` | 0 | 2026-01-20 | – |
| 5800 | `post_services` | publish | `applications` | 0 | 2025-12-15 | – |
| 5821 | `post_services` | publish | `3d-printing-contract-services` | 0 | 2026-03-06 | – |
| 12223 | `post_services` | publish | `3d-printing-contract-services-draft-blocks` | **9 ≠ 12** | 2026-07-30 | – |
| 12243 | `post_services` | draft | `pl-auto-3d-printing` | 12 | this run | ✅ |

**CNC Micromachining.html** — 11893 (`cnc-micro-machining-services`, publish, 0), 12102
(`cnc-new-temp`, draft, 0), 12127 (`cnc-micromachining-services-draft`, publish, 0), 12133
(`cnc-micromachining-services-draft-blocks`, publish, **10**, and the source of all ten original
fragments), 12239 ✅.

**Laser Micromachining.html** — 1421 (`laser-micromachining`, publish, 0), 2269
(`laser-micromachining-welding`, publish, 0), 1552 / 1592 (`post_application`, draft, 0), 3623
(`page`, publish, 0), 12226 (`laser-micromachining-services-draft-blocks`, publish, **9 ≠ 12**),
12240 ✅.

**Micro-Hole Drilling.html** — 2692 (`micro-hole-drilling`, publish, 0), 8413
(`catheter-hole-drilling`, `post_application`, publish, 0), 12225
(`laser-micro-hole-drilling-services-draft-block…`, publish, **9 ≠ 12**), 12241 ✅.

Pattern for a human deciding what to retire, redirect or promote: each service has **three**
generations — a live page with no Elementor data, a `*-draft-blocks` page from the hand-built work
carrying 9 sections, and this pipeline's 12-section `pl-auto-` draft. Only the `pl-auto-` posts
carry `_pl_auto_page`. Notably the `*-draft-blocks` pages are **published**, not drafts. Nothing
above was modified, and nothing this run did not create was written to.

## Findings for a human

1. **Posts 12240 and 12241 render `services-image-cards` wrongly** — the same token-grouping
   defect this run hit at iteration 1. On each, cards 1–3 show the next card's description in
   their link slot and cards 4–5 have no description at all. They were **not corrected**: §6
   permits writes only to posts this run created. The fix is a two-line change to each spec's
   token map (interleave per card) plus a rebuild. Post 12239 is unaffected — its spec does not
   use the pattern.
2. **`PATTERNS.md`'s `services-image-cards` entry should state the interleaving explicitly.**
   Its `Recognise when` is fine; its `Tokens` field is what let two specs guess wrong. Not edited
   here — §8 forbids touching existing entries, and §7 was not triggered.
3. **The zip is now marked `built`.** Every page of the manifest's active phase is built, which is
   what §4 requires — but a zip marked `built` is never reprocessed. Advancing to RUN 2
   (About Our Group, Contact, Project Gallery, Services & Applications) or RUN 3 (CCIT,
   sector-medical) needs the human reset the manifest describes: uncomment the phase **and** delete
   this zip's entry from `build-state.json`.
4. **The five interactive clusters may carry stale explorer copy** (see §3.c.iv above). Worth a
   diff of each file's `APPS` object against the design before go-live.
5. **Go-live is still a human step**: rename `pl-auto-<slug>` → the intended slug, and decide what
   happens to the two older generations listed in the drift report.

## Rollback map

`built/run-20260806-173742.manifest.json`. This run created exactly: posts **12242** and **12243**,
and two files — `novamira-drafts/pl-auto-rapid-prototyping.elementor.json` and
`novamira-drafts/pl-auto-3d-printing.elementor.json`. No attachments, no global operations, no
writes to anything it did not create.
