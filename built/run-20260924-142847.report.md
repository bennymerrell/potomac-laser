# Run 20260924-142847 — supervised: swap the closing band on 9 earlier drafts

**Requested in chat:** "merge and swap the closing band on the earlier drafts" (after run 20260924-135039 found the library
`cta-band-dark` 10px taller than the designs' closing band). Before the swap, `build/potomac-laser-fab7225c` was merged to
`main` (`9ee3e8b`).

**Target:** potomac-laser.com, LIVE. Nothing was published and there was no global operation. SKILL §1 normally bars
modifying posts a run did not create. This pass ran only on the user's explicit request, and only on the closing section
of these nine pipeline drafts. The service drafts 12239–12243 were **not** touched.

**§0:** UpdraftPlus DB set `1790244496` (2026-09-24 10:08:16 UTC, nonce `ec283efbb074`), verified at 4.35 h old with
`success = 1` and no errors; no backup was taken. `novamira-localhost` is disabled.

## Change

The last section of each post (`cta-band-dark`) was replaced with `cta-band-dark-display` assembled from
`reference/snippets/cta-band-dark-display.json`, with fresh element ids.

- **Token values** were carried over from each post's old band, so the link-pass URLs and brand normalisation are
  preserved. They were cross-checked against each design's `.final-cta`: same eyebrow, H2 and actions. Gallery's second
  action is a `<button>` in the design.
- **Guards:** each post had to be a `page` draft stamped by run 20260924-100754 or 20260924-125928, its last section had to
  hash to the value read beforehand, and the new ids had to be absent from the rest of the page.
- **Rollback:** all nine old sections were saved to `rollback-old-cta.json` before the first write.
- After each write, the §4 invalidation ran for that post only.

| Post | Page | Band after | Design |
|---|---|---|---|
| 12268 | About Our Group | **542** | 542 (2-line H2) |
| 12277 | About Potomac | **453** | 453 |
| 12295 | Case Studies | **453** | 453 |
| 12296 | News | **453** | 453 |
| 12298 | Employment | **453** | 453 |
| 12300 | FAQs | **453** | 453 |
| 12301 | Quality Policy | **453** | 453 |
| 12357 | Project Gallery | **542** | 541 (2-line H2) |
| 12371 | Services & Applications | **483** | 483 (3-line lede) |

Before the swap only FAQs was measured (463px, run 20260924-135039). Verified in the user's Chrome (own tab group, closed afterwards) against the mocks at the same width. H2 is 56px on all nine,
both buttons render, and no literal tokens appear. Services & Applications now matches its design in every section
(597/987/1347/279/556/483 vs 597/987/1347/279/555/483). Section counts are unchanged, and the live `/`, CNC service,
`/contact/`, `/about/` and `/project-gallery/` pages return 200.

## Repo

- The nine specs' last section now names `cta-band-dark-display` (same token names and values); `validate_spec
  --automation` is clean on all 24 specs.
- **Not edited:** PATTERNS.md. SKILL §8 bars editing existing entries, so `cta-band-dark-display`'s "Used by" still lists
  only the Materials pages. These nine posts are recorded here and in the manifest.

## Still open

- Service drafts 12239–12243 still close with `cta-band-dark`; they were not compared against their designs and remain off
  limits to the autonomous run (supervised refresh).
- The pre-existing spec/post URL differences on 12268/12277 (their links were never re-pointed to the drafts) are unchanged.

## Rollback

`built/run-20260924-142847.manifest.json` lists the nine posts with old and new section ids. To undo, put each post's
`section` from `built/run-20260924-142847.rollback-old-cta.json` (SHA-256 `e56d40e0…bb96bb3`, also at
`novamira-drafts/run-20260924-142847/`) back at its `index`, then run the §4 per-post invalidation.
