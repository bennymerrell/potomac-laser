# Page manifest — which pages of a design zip get built (AUTOMATION.md §3a)
#
# This file lives in the REPO, not inside the zip: a re-export from Claude Design
# replaces the zip wholesale, and curation that lived inside it would be lost.
# Here it is version-controlled, reviewable in a diff, and survives re-exports.
#
# BINDING — the coordinator matches a zip to its manifest by these two directives.
# `zip:` (filename) is authoritative; `zip_sha256:` is advisory. If the hash has
# moved but the filename still matches, the manifest is USED and the run reports
# "manifest predates this zip revision — re-check for new pages".
#
# zip: Potomac Laser.zip
# zip_sha256: 2f5215b42d491dae3eb7653c0d4e87c985723f7054b2916dc1937a8af331e14e
#
# Format:  <path relative to zip root>[  <post_type>][  interactive=<file>]
#   post_type is `page` (default), `post_services` or `post_application` —
#   nothing else (SKILL.md §1). Type follows what the page IS, because it sets
#   the permalink and the theme template: services go to post_services
#   (/services/<slug>/), application and sector pages to post_application.
#   Filenames contain spaces, so key=value fields are pulled out first, then the
#   type is read as the LAST remaining token; if that isn't a post type, the
#   whole remainder is the path.
#
#   interactive=<file> names a SELF-CONTAINED interactive cluster already in
#   wp-content/uploads/novamira-drafts/ (AUTOMATION.md §3.c.iv). The abbreviated
#   filenames cannot be derived from the design filename, which is why they are
#   declared here. The run REFERENCES these files and never modifies them.
#
# This list is COMPLETE: anything not listed is not built. Any HTML file in the
# zip that is missing from this list is REPORTED as unlisted, so a re-export that
# adds pages cannot be silently dropped.
#
# Drafts are created at pl-auto-<slug> and stamped _pl_auto_page = the path
# below, which is this page's durable identity across runs (AUDIT.md B5).
#
# ---------------------------------------------------------------------------
# `Potomac Laser.zip` (origin/project-zip, 12,249,689 bytes)
# 23 HTML files in the zip; 11 are pages. Drafted 2026-08-06.
#
# PHASING (AUDIT.md B4): §7 caps new patterns at 8 per zip and this zip needs
# 12-14. To run the service pages alone first (2 new patterns), comment out the
# six non-service entries below; uncomment them for run 2 and run 3.
# ---------------------------------------------------------------------------

# Service pages — 12 sections each, built from the shared CNC template.
# The "Select your application" explorer ships INLINE in these design files (the
# APPS JS object), but a built, tested, self-contained version of each already
# exists on the server from the earlier hand-built work — declared below via
# interactive=. The run references those files; it never extracts the explorer
# from the design or edits the server copies.
#
# CAVEAT: those files date from 2026-07-30/31 and were built for the PREVIOUS
# iteration of these pages. This design rewrites per-page explorer content (see
# the zip's own CLAUDE.md: APPS summaries, chips, materials). So the explorer
# will WORK but may carry stale copy until someone diffs the APPS data. The run
# reports this rather than assuming it is fine.
CNC Micromachining.html                        post_services  interactive=cnc-interactive.html
Laser Micromachining.html                      post_services  interactive=lm-interactive.html
Micro-Hole Drilling.html                       post_services  interactive=mhd-interactive.html
Rapid Prototyping.html                         post_services  interactive=rp-interactive.html
3D Printing.html                               post_services  interactive=3dp-interactive.html

# Standard pages.
About Our Group.html                           page
Contact.html                                   page
Project Gallery.html                           page
Services & Applications.html                   page

# Application / sector pages — post_application, matching their live siblings
# (e.g. 4690 "Laser Hole Drilling of Kapton", 1592, 1552). CCIT is container
# closure integrity testing; sector-medical is the medical-device sector page.
CCIT.html                                      post_application
uploads/sector-medical-potomac.html            post_application

# ---------------------------------------------------------------------------
# Deliberately NOT built, and why. Kept here so the next curator can see the
# reasoning rather than re-deriving it.
#
#   3D Printing (standalone).html                 duplicate of 3D Printing.html
#   uploads/CNC Micromachining (standalone).html  duplicate; byte-identical to
#   uploads/cnc-standalone-upload.html            each other (2,548,462 b)
#   uploads/cnc-unpacked.html                     working copy
#   uploads/Project Gallery (standalone).html     duplicate
#   design_handoff_about_page/
#     About Our Group -no hero-.html              hero-less variant
#   Blog Index.dc.html                            design canvas (<x-dc>)
#   Blog Article.dc.html                          design canvas
#   Kapton.dc.html                                design canvas (blog draft)
#   Site Footer.dc.html                           footer partial, 0 sections
#   Canvas-4.dc.html                              empty canvas, 206 bytes
#   _src/original.html                            source artefact
#
# Blog Index / Blog Article are real intent, not junk — they are just canvases
# rather than pages, and no fragment covers a blog index or article layout yet.
# Add them once the library does.
# ---------------------------------------------------------------------------
